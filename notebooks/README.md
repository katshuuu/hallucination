# Notebooks

Сюда можно положить воспроизводимые эксперименты (EDA, калибровка порога, PR-AUC на валидации).

Публичный `knowledge_bench_public.csv` используйте только для отладки и подбора архитектуры, не как единственный train.
